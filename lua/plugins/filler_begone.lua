-- filler-begone.nvim: Remove unnecessary filler lines when scrolling past end of buffer
return {
  'saghen/filler-begone.nvim',
  lazy = false,  -- Initialize automatically
  priority = 500, -- Load early but after essential plugins
}